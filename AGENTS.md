# Project Rules

- Any subpage must include a visible "返回首页" link that navigates back to the site homepage.
- Except for the root `index.html`, every HTML page must live inside an appropriate feature folder.
